import React from 'react';

import { MenuAreaStyled } from './styled.module';

const MenuArea = () => {
  return (
    <MenuAreaStyled>
      Menu
    </MenuAreaStyled>
  );
};

export default MenuArea;